{{- if .Values.config.externalSecret.enabled -}}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "chartname.fullname" . }}
  labels:
    {{- include "chartname.labels" . | nindent 4 }}
spec:
  {{- with .Values.config.externalSecret.refreshInterval }}
  refreshInterval: {{ . }}
  {{- end }}
  secretStoreRef:
    name: {{ .Values.config.externalSecret.secretStoreRef.name }}
    kind: {{ .Values.config.externalSecret.secretStoreRef.kind }}
  target:
    name: {{ .Values.config.externalSecret.target.name | default (include "chartname.fullname" .) }}
    {{- with .Values.config.externalSecret.target.creationPolicy }}
    creationPolicy: {{ . }}
    {{- end }}
  {{- with .Values.config.externalSecret.data }}
  data:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.config.externalSecret.dataFrom }}
  dataFrom:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
