{{- if eq .Values.workload.kind "Deployment" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chartname.fullname" . }}
  labels:
    {{- include "chartname.labels" . | nindent 4 }}
    {{- with .Values.extraLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.workload.deployment.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.workload.replicaCount }}
  {{- end }}
  {{- with .Values.workload.updateStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  revisionHistoryLimit: {{ .Values.workload.revisionHistoryLimit | default 3 }}
  selector:
    matchLabels:
      {{- include "chartname.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.workload.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.observability.openTelemetry.instrumentation.enabled }}
      annotations:
        instrumentation.opentelemetry.io/inject-{{ .Values.observability.openTelemetry.instrumentation.language }}: "true"
      {{- end }}
      labels:
        {{- include "chartname.selectorLabels" . | nindent 8 }}
        {{- with .Values.extraLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "chartname.serviceAccountName" . }}
      {{- with .Values.workload.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.workload.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.workload.terminationGracePeriodSeconds }}
      {{- end }}
      {{- with .Values.workload.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      {{- with .Values.workload.schedulerName }}
      schedulerName: {{ . | quote }}
      {{- end }}
      {{- with .Values.workload.dnsPolicy }}
      dnsPolicy: {{ . }}
      {{- end }}
      {{- with .Values.workload.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workload.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workload.initContainers }}
      initContainers:
        {{- tpl (toYaml .) $ | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.workload.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.workload.workingDir }}
          workingDir: {{ . | quote }}
          {{- end }}
          {{- with .Values.workload.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.workload.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.workload.image.repository }}:{{ .Values.workload.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.workload.image.pullPolicy }}
          {{- if .Values.workload.containerPorts }}
          ports: {{- include "chart.render" (dict "value" .Values.workload.containerPorts "context" $) | nindent 12 }}
          {{- else if (or .Values.service.enabled .Values.observability.serviceMonitor.enabled) }}
          ports:
          {{- if .Values.service.ports }}
          {{- range $port := .Values.service.ports }}
          {{ $p := fromYaml (include "chart.render" (dict "value" $port "context" $)) }}
            - name: {{ $p.name }}
              containerPort: {{ $p.port }}
              protocol: {{ $p.protocol | default "TCP" }}
          {{- end }}
          {{- else }}
            - name: {{ .Values.service.name | default "http" }}
              containerPort: {{ .Values.service.port }}
              protocol: {{ .Values.service.protocol | default "TCP" }}
          {{- end }}
          {{- end }}
          {{- with .Values.workload.env }}
          env: {{- include "chart.render" (dict "value" .Values.workload.env "context" $) | nindent 12 }}
          {{- if .Values.workload.extraRefEnvVars -}}
          {{- if kindIs "map" .Values.workload.extraRefEnvVars -}}
            {{- range $key, $value := .Values.workload.extraRefEnvVars }}
            - name: {{ include "chart.render" (dict "value" $key "context" $) }}
              valueFrom:
                fieldRef:
                  fieldPath: {{ include "chart.render" (dict "value" $value "context" $) }}
            {{- end }}
          {{- end }}
          {{- end }}
          {{- if .Values.workload.extraEnvVars }}
          {{- if kindIs "map" .Values.workload.extraEnvVars -}}
            {{- range $key, $value := .Values.workload.extraEnvVars }}
            - name: {{ include "chart.render" (dict "value" $key "context" $) }}
              {{- if kindIs "map" $value -}}
              {{ include "chart.render" (dict "value" $value "context" $) | nindent 14 }}
              {{- else }}
              value: {{ include "chart.render" (dict "value" $value "context" $) | quote}}
              {{- end }}
            {{- end }}
          {{- else }}
          {{- include "chart.render" (dict "value" .Values.workload.extraEnvVars "context" $) | nindent 12 -}}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- if or .Values.config.extraEnvFromConfigMap .Values.config.extraEnvFromSecret .Values.config.sealedSecrets.enabled }}
          envFrom:
            {{- range .Values.config.extraEnvFromConfigMap }}
            - configMapRef:
                name: {{ tpl . $ }}
            {{- end }}
            {{- range .Values.config.extraEnvFromSecret }}
            - secretRef:
                name: {{ tpl . $ }}
            {{- end }}
            {{- if .Values.config.sealedSecrets.enabled }}
            - secretRef:
                name: {{ include "chartname.fullname" . }}
            {{- end }}
          {{- end }}
          {{- if and .Values.workload.probes.startup .Values.workload.probes.startup.enabled }}
          startupProbe:
            {{- $probe := omit .Values.workload.probes.startup "enabled" }}
            {{- if or $probe.tcpSocket $probe.exec $probe.grpc }}
            {{- $probe = omit $probe "httpGet" }}
            {{- end }}
            {{- toYaml $probe | nindent 12 }}
          {{- end }}          
          {{- if and .Values.workload.probes.liveness .Values.workload.probes.liveness.enabled }}
          livenessProbe:
            {{- $probe := omit .Values.workload.probes.liveness "enabled" }}
            {{- if or $probe.tcpSocket $probe.exec $probe.grpc }}
            {{- $probe = omit $probe "httpGet" }}
            {{- end }}
            {{- toYaml $probe | nindent 12 }}
          {{- end }}
          {{- if and .Values.workload.probes.readiness .Values.workload.probes.readiness.enabled }}
          readinessProbe:
            {{- $probe := omit .Values.workload.probes.readiness "enabled" }}
            {{- if or $probe.tcpSocket $probe.exec $probe.grpc }}
            {{- $probe = omit $probe "httpGet" }}
            {{- end }}
            {{- toYaml $probe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.workload.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.enabled }}
              {{- if .Values.persistence.claims }}
                {{- range .Values.persistence.claims }}
            - name: {{ .name }}
              mountPath: {{ .mountPath | default (printf "/data/%s" .name) }}
                {{- end }}
              {{- else }}
            - name: {{ .Values.persistence.name | default "storage" }}
              mountPath: {{ .Values.persistence.mountPath | default "/data" }}
              {{- end }}
            {{- end }}
            {{- range .Values.config.extraConfigMaps }}
            - name: {{ .name }}
              mountPath: {{ .mountPath }}
              {{- if .subPath }}
              subPath: {{ .subPath }}
              {{- end }}
              readOnly: {{ .readOnly | default true }}
            {{- end }}
            {{- range .Values.config.extraSecrets }}
            - name: {{ .name }}
              mountPath: {{ .mountPath }}
              {{- if .subPath }}
              subPath: {{ .subPath }}
              {{- end }}
              readOnly: {{ .readOnly | default true }}
            {{- end }}
            {{- with .Values.persistence.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.persistence.extraVolumeMounts }}
            {{- include "chart.render" (dict "value" .Values.persistence.extraVolumeMounts "context" $) | nindent 12 }}
            {{- end }}
          {{- with .Values.workload.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- with .Values.workload.extraContainers }}
        {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
      volumes:
        {{- if .Values.persistence.enabled }}
          {{- if .Values.persistence.claims }}
            {{- range .Values.persistence.claims }}
        - name: {{ .name }}
          persistentVolumeClaim:
            claimName: {{ include "chartname.fullname" $ }}-{{ .name }}
            {{- end }}
          {{- else }}
        - name: {{ .Values.persistence.name | default "storage" }}
          persistentVolumeClaim:
            claimName: {{ default (include "chartname.fullname" .) .Values.persistence.existingClaim }}
          {{- end }}
        {{- end }}
        {{- range .Values.config.extraConfigMaps }}
        - name: {{ .name }}
          configMap:
            name: {{ .existingName | default .name }}
            defaultMode: {{ .defaultMode | default 420 }}
        {{- end }}
        {{- range .Values.config.extraSecrets }}
        - name: {{ .name }}
          secret:
            secretName: {{ .existingName | default .name }}
            defaultMode: {{ .defaultMode | default 420 }}
        {{- end }}
        {{- with .Values.persistence.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.persistence.extraVolumes }}
        {{- include "chart.render" (dict "value" .Values.persistence.extraVolumes "context" $) | nindent 8 }}
        {{- end }}
      {{- with .Values.workload.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
        {{- include "chartname.affinity" . | nindent 8 }} 
      {{- with .Values.workload.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end -}}
