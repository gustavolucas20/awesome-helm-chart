{{- if .Values.cronjob.enabled }}
{{- range $job := .Values.cronjob.jobs }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "chartname.fullname" $ }}
  labels:
    {{- include "chartname.labels" $ | nindent 4 }}
    app.kubernetes.io/component: cronjob
    cronjob: {{ $job.name }}
spec:
  schedule: {{ $job.schedule | quote }}
  concurrencyPolicy: {{ $job.concurrencyPolicy | default "Forbid" }}
  {{- if $job.successfulJobsHistoryLimit }}
  successfulJobsHistoryLimit: {{ $job.successfulJobsHistoryLimit | int | default 3 }}
  {{- end }}
  {{- if $job.failedJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ $job.failedJobsHistoryLimit | int | default 1 }}
  {{- end }}
  suspend: {{ $job.suspend | default false }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "chartname.selectorLabels" $ | nindent 12 }}
            app.kubernetes.io/component: cronjob
            cronjob: {{ $job.name }}
          {{- with $job.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if $.Values.observability.openTelemetry.instrumentation.enabled }}
            instrumentation.opentelemetry.io/inject-{{ $.Values.observability.openTelemetry.instrumentation.language }}: "true"
          {{- end }}
        spec:
          {{- with $.Values.global.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          serviceAccountName: {{ ($job.serviceAccount | default dict).name | default (include "chartname.serviceAccountName" $) }}
          securityContext:
            {{- toYaml ($job.podSecurityContext | default $.Values.workload.podSecurityContext) | nindent 12 }}
          restartPolicy: {{ $job.restartPolicy | default "OnFailure" }}
          {{- with $job.initContainers }}
          initContainers:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
          {{- range $container := $job.containers }}
            - name: {{ $container.name }}
              securityContext:
                {{- toYaml ($container.securityContext | default $.Values.workload.securityContext) | nindent 16 }}
              image: "{{ $container.image }}"
              imagePullPolicy: {{ $container.imagePullPolicy | default "IfNotPresent" }}
              {{- with $container.command }}
              command:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- with $container.args }}
              args:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              resources:
                {{- toYaml ($container.resources | default $.Values.workload.resources) | nindent 16 }}
              volumeMounts:
                {{- with $container.volumeMounts }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
                {{- if $.Values.configuration.enabled }}
                {{- $path := required "A valid configuration.path is required!" $.Values.configuration.path }}
                {{- $filename := required "A valid configuration.filename is required!" $.Values.configuration.filename }}
                {{- $mountname := required "A valid configuration.mountname is required!" $.Values.configuration.mountname }}
                - name: {{ $mountname }}
                  mountPath: {{ $path }}
                  subPath: {{ $filename }}
                {{- end }}
              {{- with $container.env }}
              env:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- if $.Values.workload.extraRefEnvVars -}}
              {{- if kindIs "map" $.Values.workload.extraRefEnvVars -}}
                {{- range $key, $value := $.Values.workload.extraRefEnvVars }}
                - name: {{ include "chart.render" (dict "value" $key "context" $) }}
                  valueFrom:
                    fieldRef:
                      fieldPath: {{ include "chart.render" (dict "value" $value "context" $) }}
                {{- end }}
              {{- end }}
              {{- end }}
              {{- if $.Values.workload.extraEnvVars }}
              {{- if kindIs "map" $.Values.workload.extraEnvVars -}}
                {{- range $key, $value := $.Values.workload.extraEnvVars }}
                - name: {{ include "chart.render" (dict "value" $key "context" $) }}
                  {{- if kindIs "map" $value -}}
                  {{ include "chart.render" (dict "value" $value "context" $) | nindent 18 }}
                  {{- else }}
                  value: {{ include "chart.render" (dict "value" $value "context" $) | quote}}
                  {{- end }}
                {{- end }}
              {{- else }}
              {{- include "chart.render" (dict "value" $.Values.workload.extraEnvVars "context" $) | nindent 16 -}}
              {{- end }}
              {{- end }}
              {{- with $container.envFrom }}
              envFrom:
                {{- toYaml . | nindent 16 }}
              {{- end }}
          {{- end }}
          volumes:
            {{- if $.Values.configuration.enabled }}
            - name: {{ $.Values.configuration.mountname }}
              configMap:
                name: {{ $.Values.configuration.mountname }}
                defaultMode: {{ $.Values.config.configuration.defaultMode }}
            {{- end }}
            {{- with $job.volumes }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with $job.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if $job.affinity }}
          affinity: {{- include "chart.render" (dict "value" $job.affinity "context" $) | nindent 12 }}
          {{- else }}
          affinity:
            podAffinity: {{- include "common.affinities.pods" (dict "type" $.Values.workload.podAffinityPreset "context" $) | nindent 14 }}
            podAntiAffinity: {{- include "common.affinities.pods" (dict "type" $.Values.workload.podAntiAffinityPreset "component" "cronjob" "context" $) | nindent 14 }}
            nodeAffinity: {{- include "common.affinities.nodes" (dict "type" $.Values.workload.nodeAffinityPreset.type "key" $.Values.workload.nodeAffinityPreset.key "values" $.Values.workload.nodeAffinityPreset.values) | nindent 14 }}
          {{- end }}
{{- end }}
{{- end }}
