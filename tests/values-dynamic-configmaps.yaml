# Test case for dynamic ConfigMap scanning
workload:
  kind: Deployment

config:
  # This will scan helm/files/ and create ConfigMaps for each subfolder
  configFilesPath: "files"
# Also test with environment-specific directory
# config:
#   configFilesPath: "files-dev"
